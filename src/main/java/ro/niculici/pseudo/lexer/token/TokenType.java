package ro.niculici.pseudo.lexer.token;

public enum TokenType {
    COMMA,
    PLUS,
    MINUS,
    STAR,
    DIVIDE,
    INTEGER_DIVIDE,
    MODULO,
    ASSIGNMENT,
    OPEN_BRACKET,
    CLOSE_BRACKET,
    OPEN_SQUARE_BRACKET,
    CLOSE_SQUARE_BRACKET,
    READ,
    WRITE,
    IF,
    ELSE,
    LESS,
    LESS_OR_EQUAL,
    GREATER,
    GREATER_OR_EQUAL,
    EQUAL,
    NOT_EQUAL,
    AND,
    OR,
    NOT,
    FOR,
    WHILE,
    WHILE_1,
    WHILE_2,
    REPEAT,
    THEN,
    EXECUTE,
    UNTIL,
    UNTIL_1,
    UNTIL_2,
    END,
    STRING,
    INTEGER,
    REAL,
    IDENTIFIER,
    EOL,
    EOF
}
